function [X_norm, mu] = normalize_features(X)
% mormalize_features mean normalizes the features X 

% Input: 
% X: Features X

% Outputs
% X_norm: Mean normalized version of X

%% Please insert your code here

% Return the following variables correctly
X_norm=zeros(size(X,1),size(X,2));
mu=zeros(1,size(X,2));

end
