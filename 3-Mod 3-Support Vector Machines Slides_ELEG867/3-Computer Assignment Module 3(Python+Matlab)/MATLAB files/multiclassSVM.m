function [v] = multiclassSVM(X, y, U, num_labels)
% Multiclass SVM applies the one-vs-all approach for SVM multiclass
% classification

% Inputs:

% X: Matrix containing training examples
% y: Vector containing the labels corresponding to the training examples
% U: Matrix containing the testing examples
% num_labels: Number of class labels

% Output:
% pred: Predicted labels for the testing examples

v=zeros(size(U,1),1);

% ========Your code starts here======= %



end
