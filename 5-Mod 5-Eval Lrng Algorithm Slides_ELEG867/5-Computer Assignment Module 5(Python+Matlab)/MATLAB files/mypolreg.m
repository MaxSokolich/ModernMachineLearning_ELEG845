function beta_poly = mypolreg(X,Y,p)
% This function should implement gradient descent to solve for polynomial
% regression

% Inputs:
% X: Observations
% Y: Model outputs Y=Beta(1)+X^(1)*beta(2)+...+X^(p)*beta(p+1)+noise

% Outputs
% beta_poly: Polynomial regression coefficients

% ======= Your code here ========%
beta_poly=zeros(1,p+1);
end

